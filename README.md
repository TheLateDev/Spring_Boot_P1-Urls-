# Spring_Boot_P1-Urls-
A URL shortener is a web service or application that converts long, complex web addresses into shorter, more manageable links. These shortened URLs redirect users to the original destination when clicked.
